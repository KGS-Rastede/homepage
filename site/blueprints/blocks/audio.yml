# /site/blueprints/blocks/audio.yml
name: Audio-Datei
icon: file-audio
label: "{{ name }} - {{ creator }}"
fields:
  soundfile:
    type: files
    limit: 1
  name:
    type: text
  creator:
    type: text